{
    "main": "lib/index",
    "types": "lib/index",
    "scripts": {
        "start": "npm run build:live",
        "start:dev": "ts-node src/main.ts",
        "build": "tsc -p .",
        "build:live": "nodemon",
        "test": "jest --watch"
    },
    "devDependencies": {
        "@types/jest": "^23.3.12",
        "@types/node": "^10.12.18",
        "jest": "^26.0.1",
        "nodemon": "^2.0.4",
        "ts-jest": "^26.0.0",
        "ts-node": "^7.0.1",
        "typescript": "^3.2.4"
    }
}
